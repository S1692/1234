# Example environment variables for local development

# Service database connection URL (IPv4 compatible Session/Transaction Pooler)
DATABASE_URL=

# Gateway target service base URL (e.g. http://service:8000 for docker-compose or https://<service-app>.railway.app in production)
SERVICE_BASE_URL=

# Frontend API base URL (e.g. http://localhost:8000 for local or https://<gateway-app>.railway.app in production)
NEXT_PUBLIC_API_BASE=
